[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn hotel.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
